document.addEventListener('DOMContentLoaded', () => { if (typeof lucide !== 'undefined') lucide.createIcons(); const elements = { hamburger: document.getElementById('hamburger'), closeBtn: document.getElementById('closeDrawer'), drawer: document.getElementById('mobileDrawer'), overlay: document.getElementById('drawerOverlay'), themeBtns: document.querySelectorAll('.theme-toggle'), reloadBtn: document.getElementById('reloadBtn'), menuIcon: document.getElementById('hamburgerMenuIcon'), closeIcon: document.getElementById('hamburgerCloseIcon'), html: document.documentElement }; if (elements.hamburger && elements.drawer) { const toggleDrawer = (isOpen) => { const method = isOpen ? 'add' : 'remove'; elements.drawer.classList[method]('open'); elements.overlay.classList[method]('open'); elements.menuIcon?.classList.toggle('hidden', isOpen); elements.closeIcon?.classList.toggle('hidden', !isOpen); elements.hamburger.setAttribute('aria-label', isOpen ? 'Close menu' : 'Open menu'); document.body.style.overflow = isOpen ? 'hidden' : ''; }; elements.hamburger.addEventListener('click', () => { const isOpen = elements.drawer.classList.contains('open'); toggleDrawer(!isOpen); }); [elements.closeBtn, elements.overlay].forEach(el => el?.addEventListener('click', () => toggleDrawer(false))); window.addEventListener('resize', () => { if (window.innerWidth >= 1024 && elements.drawer.classList.contains('open')) { toggleDrawer(false); } }); } const setTheme = (theme) => { elements.html.classList.toggle('dark', theme === 'dark'); localStorage.theme = theme; }; elements.themeBtns.forEach(btn => { btn.addEventListener('click', (e) => { e.preventDefault(); const isDark = elements.html.classList.contains('dark'); setTheme(isDark ? 'light' : 'dark'); }); }); window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', e => { if (!localStorage.theme) setTheme(e.matches ? 'dark' : 'light'); }); elements.reloadBtn?.addEventListener('click', () => window.location.reload()); const topNav = document.getElementById('topNav'); if (topNav) { const updateNavState = () => { topNav.classList.toggle('nav-glass', window.scrollY > 40); }; updateNavState(); window.addEventListener('scroll', updateNavState, { passive: true }); } });